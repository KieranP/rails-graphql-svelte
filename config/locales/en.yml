en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            password:
              invalid: "must be at least 8 characters long and contain at least one number, lower case letter, upper case letter and symbol"
