// QRCode is an external library which doesn't provide their own TypeScript definitions
// As a result, we just define it as a module and skip having TS definitions for it
declare module 'qrcode/build/qrcode'
